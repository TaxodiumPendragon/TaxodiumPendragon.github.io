---
title: 章节作业 5：二叉树
description: 二叉树遍历、二叉搜索树、堆与 Huffman 树。
course: data-structures-and-algorithms
category: homework
order: 5
source_file: homework/5二叉树.md
tags:
  - binary-tree
  - heap
sidebar:
  order: 5
prev:
  link: /courses/data-structures-and-algorithms/homework/04-strings/
  label: 章节作业 4：字符串
next:
  link: /courses/data-structures-and-algorithms/homework/06-tree/
  label: 章节作业 6：树与并查集
---
## 0.Review

- n个结点的二叉树有多少个不同的形态（卡特兰数）

已知前序和中序如何求解树
1. 确定树的根节点：树根是当前树中所有元素在前序序列中的第一个元素
2. 求解树的子树：找出**根节点在中序序列**中的位置，根左边的所有元素就是左子树，根右边的所有元素就是右子树。
 - - 若根节点左边或右边为空，则该方向子树为空
 - - 若根节点左边和右边都为空，则根节点为叶节点
3. 递归求解树：将左、右子树分别看成一棵二叉树，
4. 重复上述步骤，直到所有节点完成定位。

### 树的周游

- 已知二叉树的先序序列和后序序列，**不能唯一确定一棵二叉树**。只有知道中序加其他组合才能 

- 递归实现：区别仅在于前中后的访问函数放的就分别是**前中后的位置

```cpp
void depthOrder(Tree<T> * root){
    if(root!=NULL){
        Visit(root)//前序preorder
        depthOrder(root->leftchild());
        Visit(root)//中序order
        depthOrder(root->rightchild());
        Visit(root)//后序order
    }
}
```

- 非递归实现

### 特殊树

> 树里的度和图论中的度数并非一个概念，这里是指子节点

- **满二叉树**：若节点或为树叶（0度节点），或为两棵非空子树（2度节点），则称作满二叉树。*总是满* 
- **完全二叉树**允许最后一层不满员，但必须遵守 “从左到右依次填充” 的规则。这个特性使得完全二叉树非常适合用数组来存储，因为数组下标可以直接对应树的编号，不会浪费空间
- **堆**一定是完全二叉树

Huffman树：k叉时，虚节点由 $(m-1)%(k-1)$得出，否则需要$(k-1)-(m-1)%(k-1)$ 个虚节点

### 二叉搜索树

- 建立二叉搜索树，可以从一个空的二叉搜索树开始，将关键码一个个插进去。左边小右大

### 最小/大堆

- **插入法建堆**：自顶向下逐个插入
- **自底向上筛选法建堆**：按所给顺序形成完全二叉树，随后从*最后一个非叶子节点*开始往前遍历到根做下沉（包含自己的叶子节点，是最后比较的应该），*一次筛选仅改变本节点与俩子节点*，
- - **建堆的时候千万不要往上换，siftDown**
- **删除节点**：删除节点则与最后一个节点交换，自顶向下筛选

## 1.补全

![补全后的图](/assets/courses/data-structures-and-algorithms/homework/binaryTree.png)

## 2.证明

> 一棵二叉树的所有终端结点（叶节点），在前序序列、中序序列、后序序列中都按相同的相对位置出现。//取最小单元证明即可

设二叉树 $T$ 有 $n$ 个叶节点 $L_1, L_2, \dots, L_n$,可知对于任意两个叶节点 $L_i$ 和 $L_j$，设它们的最低公共祖先为 $p$

如果 $L_i$ 在 $p$ 的左子树，$L_j$ 在 $p$ 的右子树：

- 对前序遍历：左子树节点 → 右子树节点
- 对中序遍历：左子树节点 → $p$ → 右子树节点
- 对后序遍历：左子树节点 → 右子树节点 → $p$

⇒ 在所有三种遍历中，$L_i$ 都在 $L_j$ 之前

又如果 $L_i$ 在 $p$ 的右子树，$L_j$ 在 $p$ 的左子树，同理可得：在所有三种遍历中，$L_j$ 都在 $L_i$ 之前

因此，对于任意叶节点对 $(L_i, L_j)$，它们在三种遍历序列中的相对顺序由它们在 LCA 处的左右关系唯一确定，因此所有叶节点在三种遍历序列中保持相同的相对顺序。

## 3.哈夫曼树

>哈夫曼树是一种基于贪心算法构建的树结构，并且带权路径长度最小，在数据编码（如文件压缩）得到了广泛应用。请基于哈夫曼树的性质，解决以下问题：


### （1）如果一棵哈夫曼树中有199个结点，请求解该哈夫曼树中叶子结点个数。

哈夫曼树是一种满二叉树，其中每个非叶子结点有两个子节点

101

### （2）假设用于通信的电文仅由8个字母组成，字母在电文中出现的频率分别为0.07，0.19，0.02，0.06，0.32，0.03，0.21，0.10。试为这8个字母设计哈夫曼编码。

### （3）使用0~7的二进制表示是另一种编码方案。对于上述实例，比较两种编码方案的优缺点。

## 4.使用堆的性质解决以下问题

### （1）已知待排序的序列为（503，87，512，61，908，170，897，275，653，462），根据以上序列建立一个堆（画出第一步和最后堆的结果图），希望先输出最小值。

done

### （2）设有一个最小堆，即堆中任意结点的关键码均大于它的左子结点和右子结点的关键码。其具有最大值的元素可能在什么地方？

一定在叶子结点

### （3）对n个元素进行初始建堆的过程中，已知时间代价为 ，即初始建堆过程中最多做$C_0 n$次数据比较， $C_0 $为常数，求$C_0$的值，并给出推导过程。


